<script lang="ts">
	/**
	 * App Layout — authenticated shell with bottom tabs (mobile) / sidebar (desktop).
	 * Auth guard (redirect to /login) wired in Task 0.6 via +layout.server.ts.
	 * Full implementation in Task 0.6.
	 */
</script>

<slot />
